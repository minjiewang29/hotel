<?php
class counter extends CI_Controller {
    function index($page){
        
        echo 'hello word';
        /*
        $this->load->database();
        $date=date("Y-m-d");
        $page=$this->db->escape($page);
        $sql="call proc_counter($page,'$date');";
        $this->db->query($sql);
         * 
         */
    }
}

